{
  "name": "notary-office-1762",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "esbuild index.tsx --bundle --outfile=main.js --jsx=automatic --loader:.tsx=tsx --external:react --external:react-dom"
  },
  "devDependencies": {
    "esbuild": "^0.20.2"
  }
}